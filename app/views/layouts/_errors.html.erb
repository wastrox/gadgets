  <% if element.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(element.errors.count, "error") %> prohibited this <%=element.class.name.titleize%> from being saved:</h2>

      <ul>
      <% element.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>